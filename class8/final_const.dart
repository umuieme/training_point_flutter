void main() {
  final int a = getRandomNumber();
  // a = 10;

  const int b = 2;
}

int getRandomNumber() {
  return 2;
}

/*

final:
The value can be assgined only once and cannot be changed. The value assign can be done after variable decration.

const:
The value must be assigned while declaring the variable and it cannot be changed. The value is determined in compile time.



*/
